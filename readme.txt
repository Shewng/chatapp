Name: Samuel Wong
UCID: 30025217


How to run:

In the folder containing all the files, run "npm install" to install dependencies.

In console, navigate to the folder, then type "node index.js" to run the server.

Finally, type "localhost:3000" in any web browser's URL, preferable Chrome or Firefox.